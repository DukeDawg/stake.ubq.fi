name: Deno Deploy

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    uses: ubiquity/deno-deploy-workflow/.github/workflows/deno-deploy-reusable.yml@main
    with:
      project: stake-ubq-fi
      deploy_platform: deno2
      entrypoint: serve.ts
      root: .deploy-root
      install_command: deno install
      build_command: |
        deno task build
        rm -rf .deploy-root
        mkdir -p .deploy-root/static
        cp -R dist/. .deploy-root/static/
        cp serve.ts deno.lock deno.jsonc .deploy-root/
      build_env: |
        SUPABASE_URL=$SUPABASE_URL
        SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
        FRONTEND_URL=https://stake.ubq.fi
      runtime_env: |
        STATIC_DIR=static
      include: |
        dist/**
      debug_fetch_fail: true
      required_env: |
        DENO_DEPLOY_TOKEN
        SUPABASE_URL
        SUPABASE_ANON_KEY
    secrets: inherit
